info:
  name: digest-auth
  type: http
  seq: 3

http:
  method: GET
  url: https://httpbin.org/digest-auth/auth/usebruno/bruno123
  auth:
    type: digest
    username: usebruno
    password: bruno123

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
