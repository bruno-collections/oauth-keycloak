info:
  name: api-key
  type: http
  seq: 4

http:
  method: GET
  url: https://httpbin.org/headers
  auth:
    type: apikey
    key: X-Api-Key
    value: usebruno
    placement: header

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
