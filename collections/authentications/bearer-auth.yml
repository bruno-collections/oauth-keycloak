info:
  name: bearer-auth
  type: http
  seq: 2

http:
  method: GET
  url: https://httpbin.org/bearer
  auth:
    type: bearer
    token: usebruno

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
